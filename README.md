# Real Time Chat and Notification

## Setup VSCode with NPM, Typescript, Node, and Express
`npm init -y` - initialize npm
`npm i typescript ts-node express` - core dependencies for the app

## install Socket.io
`npm install socket.io`

## Aditional packages bufferutil and utf-8-validate
`npm install --save-optional bufferutil utf-8-validate`